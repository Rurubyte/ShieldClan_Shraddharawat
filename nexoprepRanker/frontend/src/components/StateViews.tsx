import { AlertTriangle, FileSearch, RefreshCw } from 'lucide-react'
export function EmptyState({ title, detail }: { title: string; detail: string }) { return <div className="surface grid min-h-64 place-items-center p-8 text-center"><div><span className="mx-auto mb-4 grid size-12 place-items-center rounded-2xl bg-slate-800 text-indigo-300"><FileSearch/></span><h3 className="font-semibold">{title}</h3><p className="mt-2 max-w-sm text-sm text-slate-400">{detail}</p></div></div> }
export function ErrorState({ message, retry }: { message: string; retry?: () => void }) { return <div className="surface grid min-h-64 place-items-center p-8 text-center"><div><span className="mx-auto mb-4 grid size-12 place-items-center rounded-2xl bg-rose-500/10 text-rose-300"><AlertTriangle/></span><h3 className="font-semibold">We couldn’t reach the ranking service</h3><p className="mt-2 max-w-md text-sm text-slate-400">{message}</p>{retry && <button onClick={retry} className="btn-secondary mt-5"><RefreshCw size={16}/>Retry</button>}</div></div> }
