import { AnimatePresence, motion } from 'framer-motion'
import { FileCheck2, FileUp, Trash2, UploadCloud } from 'lucide-react'
import { useDropzone } from 'react-dropzone'
import { cn } from '../utils/cn'

interface Props { title: string; description: string; accept: Record<string, string[]>; file?: File; required?: boolean; onFile: (file?: File) => void }
export function UploadCard({ title, description, accept, file, required, onFile }: Props) { const { getRootProps, getInputProps, isDragActive, fileRejections } = useDropzone({ accept, multiple: false, onDropAccepted: ([next]) => onFile(next) }); const error = fileRejections[0]?.errors[0]?.message; return <motion.div whileHover={{ y: -3 }} className={cn('surface p-5 transition', isDragActive && 'border-indigo-400 ring-2 ring-indigo-500/20')}><div className="mb-4 flex items-start gap-3"><span className="grid size-10 shrink-0 place-items-center rounded-xl bg-indigo-500/10 text-indigo-300"><FileUp size={20}/></span><div><h2 className="font-semibold">{title}{required && <span className="ml-1 text-indigo-300">*</span>}</h2><p className="mt-1 text-sm text-slate-400">{description}</p></div></div><AnimatePresence mode="wait">{file ? <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} className="flex items-center justify-between rounded-xl border border-emerald-500/20 bg-emerald-500/5 p-3"><div className="flex min-w-0 items-center gap-2 text-sm text-emerald-200"><FileCheck2 size={18} className="shrink-0"/><span className="truncate">{file.name}</span></div><button onClick={() => onFile()} className="rounded-lg p-1.5 text-slate-400 hover:bg-slate-800 hover:text-red-300" aria-label={`Remove ${file.name}`}><Trash2 size={16}/></button></motion.div> : <motion.div key="drop" initial={{ opacity: 0 }} animate={{ opacity: 1 }}><div {...getRootProps()} className="cursor-pointer rounded-xl border border-dashed border-slate-700 px-4 py-7 text-center transition hover:border-indigo-400 hover:bg-indigo-500/5"><input {...getInputProps()}/><UploadCloud size={23} className="mx-auto mb-2 text-indigo-300"/><p className="text-sm text-slate-300">Drop file here or <span className="font-medium text-indigo-300">browse files</span></p></div></motion.div>}</AnimatePresence>{error && <p className="mt-2 text-xs text-rose-300">{error}</p>}</motion.div> }
